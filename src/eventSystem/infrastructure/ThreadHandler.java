package eventSystem.infrastructure;

import java.util.LinkedList;
import java.util.List;

public class ThreadHandler {
/*
    private List<ThreadEventListener> listenerList = new LinkedList<>();

    public void add( ThreadEventListener listener) { this.listenerList.add(listener); }

    public void remove(ThreadEventListener listener) {
        this.listenerList.remove(listener);
    }

    public void handle(ThreadEvent event){ for (ThreadEventListener listener : listenerList) { listener.onThreadEvent(event); } }
 */
}

